import React from "react";
import { View, Text, StyleSheet } from "react-native";

const GalleryScreen = () => {
  return <Text style={{ fontSize: 48 }}>Gallery Screen</Text>;
};

const styles = StyleSheet.create({});

export default GalleryScreen;
