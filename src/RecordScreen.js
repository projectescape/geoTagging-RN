import React from "react";
import { View, Text, StyleSheet } from "react-native";

const RecordScreen = () => {
  return <Text style={{ fontSize: 48 }}>Record Screen</Text>;
};

const styles = StyleSheet.create({});

export default RecordScreen;
